let (:newNormalize, :loadBytes, ...) = //{github.com/anz-bank/sysl/pkg/arrai/sysl};

let rel = newNormalize(
	loadBytes(//[//encoding.bytes]{github.com/anz-bank/sysl/pkg/arrai/diagrams/testdata/integration-model.sysl.pb})
);

let param = (models: [(:rel)]);

let plugin = //{./integration_model_plugin};

(
	integration_model: plugin(param).output = //[//encoding.bytes]{./testdata/integration-model.json}
)
