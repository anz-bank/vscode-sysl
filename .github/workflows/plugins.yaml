name: Plugins

on: [pull_request]

jobs:
  build-and-test:
    name: Build and test plugins
    runs-on: ubuntu-latest

    steps:
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.16

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      - name: Check out code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build protocol and plugins
        run: yarn lerna run build --scope=vscode-sysl

      # TODO: Fix or delete once non-LSP plugins are retired.
      # - name: Check clean
      #   run: git --no-pager diff HEAD && test -z "$(git status --porcelain)"
